type: object
description: Negation of condition
required:
  - conditionType
  - condition
properties:
  conditionType:
    type: string
    enum:
      - Not
  condition:
    - $ref: ./../../condition.yaml
