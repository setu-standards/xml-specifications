# Generated by Semantic Treehouse
#
# Adapted to refer to custom types for conditions and occurrences
#
$id: "https://ontology.setu.nl/inquiry-pay-equity/InquiryPayEquity"
$schema: "https://json-schema.org/draft/2020-12/schema"
title: "SETU Inquiry Pay Equity v2.0"
description: "Generated by Semantic Treehouse on 2026-02-06T16:05:14+00:00"
type: object
required:
  - documentId
  - effectivePeriod
  - customer
  - remuneration
additionalProperties: false
properties:
  documentId:
    $ref: "#/definitions/Identifier"
  versionId:
    description: "The version identifier is a customer-specific identifier used to distinguish one version of the SETU Inquiry Pay Equity message from another."
    required:
      - value
    additionalProperties: false
    properties:
      value:
        $ref: "#/definitions/IdValueType"
    type: object
  issued:
    description: "The datetime the document was issued."
    type: string
    format: date-time
  effectivePeriod:
    $ref: "#/definitions/EffectivePeriodType"
  customer:
    $ref: "#/definitions/PartyType"
  baseDefinition:
    items:
      $ref: "#/definitions/BaseDefinitionType"
    type: array
  labourAgreements:
    $ref: "#/definitions/LabourAgreements"
  positionProfile:
    items:
      $ref: "#/definitions/PositionProfile"
    type: array
  remuneration:
    items:
      description: "Details about the remuneration package, including salary tables based on the work duration. It includes one or more salary tables for a specific work duration or characteristics."
      required:
        - origin
        - workDuration
        - interval
        - salaryScale
      additionalProperties: false
      properties:
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        workDuration:
          description: "Specifies the work duration to which the salary table applies. This consists of:\n\n- An amount block indicating the number of hours.\n- An interval block specifying the period over which these hours apply.\n- A valuePerWeek field indicating the equivalent number of hours per week, useful for calculations such as leave entitlement."
          required:
            - amount
            - interval
            - valuePerWeek
          additionalProperties: false
          properties:
            amount:
              description: "The value of the work duration in hours, expressed as decimal. E.g., 40"
              required:
                - value
                - unitCode
              additionalProperties: false
              properties:
                value:
                  description: "The value. "
                  type: number
                unitCode:
                  $ref: "#/definitions/IntervalUnitCodeType"
              type: object
            interval:
              $ref: "#/definitions/IntervalType"
            valuePerWeek:
              description: "Th field indicates the equivalent number of hours per week, useful for calculations such as leave entitlement."
              type: number
          type: object
        interval:
          $ref: "#/definitions/IntervalType"
        hourlyWageConversion:
          $ref: "#/definitions/WorkedCompensationAmount"
        salaryScale:
          items:
            $ref: "#/definitions/SalaryScaleType"
          minItems: 1
          type: array
        individualSalaryIncrease:
          items:
            description: "The individual salary review is a process where an employee's salary is evaluated and adjusted based on individual performance, and other relevant criteria. This container specifies the increase rate of these adjustments."
            additionalProperties: false
            properties:
              effectiveDate:
                $ref: "#/definitions/OccurrenceType"
              line:
                $ref: "#/definitions/ArrangementLineLimitedType"
            type: object
          type: array
        generalSalaryIncrease:
          items:
            description: "The general salary adjustment refers to the increase applied uniformly to the entire salary table. This adjustment ensures that all employees receive a consistent percentage or fixed increase in their salaries. Please mind the usage note when to use this container."
            additionalProperties: false
            properties:
              effectiveDate:
                $ref: "#/definitions/OccurrenceType"
              amount:
                description: "Container element that contains the value of the amount and the type of unit of the value."
                required:
                  - value
                  - unitCode
                additionalProperties: false
                properties:
                  value:
                    description: "The numerical value of the remuneration amount expressed as decimal. The unit type of this value can be specified in the unit code element."
                    type: number
                  unitCode:
                    $ref: "#/definitions/AmountUnitCodeType"
                type: object
              description:
                description: "Free text description of the general salary table increase."
                type: string
            type: object
          type: array
        conditions:
          items:
            $ref: "#/definitions/ConditionType"
          type: array
      type: object
    minItems: 1
    type: array
  allowance:
    items:
      description: "A container element that includes various allowances that can be granted to an employee."
      required:
        - name
        - origin
        - typeCode
      additionalProperties: false
      properties:
        id:
          $ref: "#/definitions/Identifier"
        name:
          description: "The name or description of the allowance, indicating the type of allowance and giving extra information that does not fit in any of the other fields."
          type: string
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        description:
          description: " A free‑text field for capturing additional details related to reward element (NL: beloningscomponent), such as specifics, limitations and other conditions."
          type: string
          format: uri-reference
        typeCode:
          $ref: "#/definitions/AllowanceCodeType"
        period:
          items:
            $ref: "#/definitions/Period"
          type: array
        line:
          items:
            $ref: "#/definitions/ArrangementLineType"
          type: array
        reference:
          items:
            $ref: "#/definitions/AllowanceReferenceType"
          type: array
        phaseOutScheme:
          description: "If this allowance has a phasing out scheme regulation, this can be described in this element. "
          type: string
      type: object
    type: array
  holidayAllowance:
    items:
      description: "A container that contains the details of the holiday allowance arrangement."
      required:
        - origin
      additionalProperties: false
      properties:
        id:
          $ref: "#/definitions/Identifier"
        name:
          description: "The name or description of the holiday allowance, giving extra information that does not fit in any of the other fields."
          type: string
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        line:
          items:
            $ref: "#/definitions/ArrangementLineType"
          type: array
        payDate:
          $ref: "#/definitions/OccurrenceType"
      type: object
    type: array
  sickPay:
    items:
      description: "This element refers to the method in which the employer continues to compensate an employee when the employee has fallen ill. "
      required:
        - name
        - origin
      additionalProperties: false
      properties:
        id:
          $ref: "#/definitions/Identifier"
        name:
          description: "The name or description of the sick pay regulation, indicating the type of sick pay and giving extra information that does not fit in any of the other fields."
          type: string
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        waitingDays:
          $ref: "#/definitions/WaitingDaysType"
        line:
          items:
            $ref: "#/definitions/ArrangementLineType"
          type: array
      type: object
    type: array
  leave:
    items:
      $ref: "#/definitions/ArrangementType"
    type: array
  individualChoiceBudget:
    items:
      description: "Individual choice budget (Dutch: IKB) is used if the employer has an individual choice budget. It's elements are used for the different options an employee has regarding this budget as well as its costs. "
      required:
        - name
        - origin
      additionalProperties: false
      properties:
        id:
          $ref: "#/definitions/Identifier"
        name:
          description: "The name or description of the individual choice budget, indicating the type of allowance and giving extra information that does not fit in any of the other fields."
          type: string
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        line:
          items:
            $ref: "#/definitions/ArrangementLineExtendedType"
          type: array
        option:
          items:
            description: "Associates the different rates on how this individual choice budget can be spent (e.g. the percentage of salary that one extra day of vacation costs)."
            required:
              - description
            additionalProperties: false
            properties:
              description:
                description: "Detailed free text description on the method in which this budget can be spent by an employee."
                type: string
            type: object
          type: array
      type: object
    type: array
  pension:
    items:
      description: "The employer's contribution to a pension plan."
      required:
        - origin
      additionalProperties: false
      properties:
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        line:
          items:
            $ref: "#/definitions/ArrangementLineType"
          type: array
        franchise:
          description: "Indicator whether an employer makes use of a franchise."
          additionalProperties: false
          properties:
            description:
              description: "Free text description of the franchise."
              type: string
          type: object
      type: object
    type: array
  sustainableEmployability:
    items:
      description: "Sustainable employability allows for the employer to add information regarding sustainable employability arrangement that the employer might have (e.g. study budget for the employee)."
      required:
        - name
        - origin
        - typeCode
      additionalProperties: false
      properties:
        id:
          $ref: "#/definitions/Identifier"
        name:
          description: "The name or description of the sustainable employability plan.\n"
          type: string
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        typeCode:
          description: "The type of the sustainable employability plan. (e.g., personal development budget)"
          type: string
        line:
          items:
            $ref: "#/definitions/ArrangementLineExtendedType"
          type: array
        payDate:
          $ref: "#/definitions/OccurrenceType"
      type: object
    type: array
  supplementaryArrangement:
    items:
      description: "A container that specifies the supplementary arrangement(s) (In Dutch: 'aanvullende regelingen')."
      required:
        - name
        - description
        - typeCode
      additionalProperties: false
      properties:
        name:
          description: "The name of the supplementary arrangement."
          type: string
        description:
          description: "The description of the supplementary arrangement."
          type: string
        typeCode:
          description: "Classification of the supplementary arrangement (e.g. RVU or Generatiepact)."
          enum:
            - Generationpact
            - RVU
          type: string
      type: object
    type: array
  otherArrangement:
    items:
      description: "This element can be used to provide any additional arrangements that have not been covered by the other elements."
      required:
        - name
        - origin
      additionalProperties: false
      properties:
        id:
          $ref: "#/definitions/Identifier"
        name:
          description: "Free text description for the 'other' field. This is the name of the other compensations."
          type: string
        origin:
          $ref: "#/definitions/OriginType"
        effectivePeriod:
          $ref: "#/definitions/EffectivePeriodType"
        description:
          description: " A free‑text field for capturing additional details related to reward element (NL: beloningscomponent), such as specifics, limitations and other conditions."
          type: string
        line:
          items:
            $ref: "#/definitions/ArrangementLineExtendedType"
          type: array
      type: object
    type: array

definitions:
  OriginType:
    description: "Specifies the origin of the applicable agreement."
    required:
      - type
    additionalProperties: false
    properties:
      type:
        description: "The type of agreement."
        enum:
          - CollectiveLabourAgreement
          - CollectiveLabourAgreementExtended
          - CustomLabourAgreement
          - Unknown
        type: string
    type: object
  IntervalType:
    description: "This element is used to signify the interval in which this rate is applied. If this rate is applied every hour then the unit code would be 'Hour' or if this rate is applied every kilometer, the interval code would be 'Kilometer'. The value can be used to express denominations of the provided interval code. If for instance it is required that the interval is every 15 minutes, the unit code can be 'Hour' and the value will be 0.25 as a quarter of 60 minutes is 15 minutes."
    required:
      - value
      - unitCode
    additionalProperties: false
    properties:
      value:
        description: "The value can be used to express denominations of the provided interval code. If for instance it is required that the interval is every 15 minutes, the unit code can be 'Hour' and the value will be 0.25 as a quarter of 60 minutes is 15 minutes. "
        type: number
      unitCode:
        $ref: "#/definitions/IntervalCodeType"
    type: object
  ArrangementLineType:
    description: "A container that contains the details of the rate in which this pension is provided to the employee. It includes elements such as the amount that is provided (3% of monthly salary), the interval in which this is applied in the specified period (e.g. 'Hour' for an hourly application) and any conditions that might have to be met for this rate to be applicable."
    required:
      - interval
    additionalProperties: false
    properties:
      lineId:
        description: "A unique identifier of this specific line element."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            $ref: "#/definitions/IdValueType"
        type: object
      amount:
        $ref: "#/definitions/AmountType"
      interval:
        $ref: "#/definitions/IntervalType"
      conditions:
        items:
          $ref: "#/definitions/ConditionType"
        type: array
      ikbReference:
        items:
          $ref: "#/definitions/IkbReferenceType"
        type: array
    type: object
  ProportionalType:
    description: "The proportional element is used to specify whether certain work-related conditions such as leave entitlements, remuneration, or similar employment provisions are applied proportionally based on specific employee characteristics. "
    required:
      - partTimePercentage
      - employmentDuration
    additionalProperties: false
    properties:
      partTimePercentage:
        description: "Indicates whether the condition is calculated proportionally based on the employee’s part‑time percentage, typically measured against a full‑time work week (commonly 36-40 hours)."
        type: boolean
      employmentDuration:
        description: "Indicates whether proportional calculation must be applied according to the employee's length of employment."
        type: boolean
      description:
        description: "A free-text field for capturing any additional details, clarifications, or contextual information relevant to how the proportional rule is applied."
        type: string
    type: object
  Identifier:
    description: "The unique identifier of this arrangement."
    required:
      - value
      - schemeAgencyId
    additionalProperties: false
    properties:
      value:
        $ref: "#/definitions/IdValueType"
      schemeAgencyId:
        $ref: "#/definitions/SchemeAgencyIdType"
    type: object
  ArrangementLineLeaveType:
    description: "Leave granted for specific, non-routine circumstances as defined by the employer or applicable Collective Labour Agreement, such as bereavement, marriage, or relocation. The specific reasons and duration of special leave may vary"
    required:
      - amount
      - interval
    additionalProperties: false
    properties:
      name:
        description: "The name of the special leave"
        type: string
      amount:
        $ref: "#/definitions/AmountExtendedType"
      interval:
        $ref: "#/definitions/IntervalType"
      conditions:
        items:
          $ref: "#/definitions/ConditionType"
        type: array
      leaveDayValue:
        description: "The financial equivalent of one special leave day. It reflects the cost or compensation value of a single day off from work. "
        required:
          - value
          - unitCode
          - baseAmount
        additionalProperties: false
        properties:
          value:
            description: "The numerical value of the financial equivalent of one special leave day. It reflects the cost or compensation value of a single day off from work. The unit type of this value can be found in the unit code element. This unit code can be for example: 'fixed' or 'percentage', this value element would then signify the value as expressed in a money equivalent 'fixed' or certain percentage value if the unit code would be  'percentage'."
            type: number
          minValue:
            description: "If applicable, a minimum value can be provided to signify a minimum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the minimum value, the minimum value will be used instead of the value element."
            type: number
          maxValue:
            description: "If applicable, a maximum value can be provided to signify a maximum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the maximum value, the maximum value will be used instead of the value element."
            type: number
          unitCode:
            $ref: "#/definitions/AmountUnitCodeType"
          baseAmount:
            $ref: "#/definitions/BaseAmountType"
        type: object
      ikbReference:
        items:
          $ref: "#/definitions/IkbReferenceType"
        type: array
    type: object
  AmountType:
    description: "Container element that contains the value of the amount, the type of unit of the value and a base. These three elements together can be used to calculate the amount of any type of rate. If applicable, a minimum and a maximum value can also be provided to signify a minimum and maximum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the minimum value, the minimum value will replace the value element."
    required:
      - value
      - unitCode
      - baseAmount
    additionalProperties: false
    properties:
      value:
        description: "The numerical value of the remuneration amount expressed as decimal. The unit type of this value can be specified in the unit code element."
        type: number
      minValue:
        description: "If applicable, a minimum value can be provided to signify a minimum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the minimum value, the minimum value will be used instead of the value element."
        type: number
      maxValue:
        description: "If applicable, a maximum value can be provided to signify a maximum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the maximum value, the maximum value will be used instead of the value element."
        type: number
      unitCode:
        $ref: "#/definitions/AmountUnitCodeType"
      baseAmount:
        $ref: "#/definitions/BaseAmountType"
      proportional:
        $ref: "#/definitions/ProportionalType"
    type: object
  AmountExtendedType:
    description: "Container element that contains the value of the amount, the type of unit of the value and a base. These three elements together can be used to calculate the amount of any type of rate. If applicable, a minimum and a maximum value can also be provided to signify a minimum and maximum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the minimum value, the minimum value will replace the value element."
    required:
      - value
      - unitCode
      - baseAmount
    additionalProperties: false
    properties:
      value:
        description: "The numerical value of the remuneration amount expressed as decimal. The unit type of this value can be specified in the unit code element."
        type: number
      minValue:
        description: "If applicable, a minimum value can be provided to signify a minimum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the minimum value, the minimum value will be used instead of the value element."
        type: number
      maxValue:
        description: "If applicable, a maximum value can be provided to signify a maximum that this rate provides. This implies that if the amount as a combination of the value and the base and the unit code is less than the maximum value, the maximum value will be used instead of the value element."
        type: number
      unitCode:
        $ref: "#/definitions/AmountUnitCodeExtendedType"
      baseAmount:
        $ref: "#/definitions/BaseAmountType"
      proportional:
        $ref: "#/definitions/ProportionalType"
    type: object
  WorkedCompensationAmount:
    description: "Information about the conversion to regular hourly wages."
    additionalProperties: false
    properties:
      hourlyWageFactor:
        description: "A number used to calculate the hourly wage from a periodic salary (e.g., monthly salary). The periodic salary is divided by this factor to determine the hourly wage. \n\nFormula: hourly wage = periodic salary / hourlyWageFactor."
        type: number
      hourlyWagePercentage:
        description: "A percentage that the periodic salary (such as a monthly salary) is multiplied by to calculate the hourly wage.\n\nFormula: hourly wage = (periodic salary * hourlyWagePercentage) / 100."
        type: number
    type: object
  BaseAmountType:
    description: "This element stores information about the base over which the amount is calculated. If the value is 3, the unit code is 'Percentage' and the base code is 'MonthlySalary', together they would imply a 3% of monthly salary."
    required:
      - unitCode
    additionalProperties: false
    properties:
      unitCode:
        $ref: "#/definitions/BaseUnitCodeType"
      baseType:
        $ref: "#/definitions/BaseDefinitionCodeType"
      value:
        description: "This element can be used to store additional information about the value of the base if applicable."
        type: number
      minValue:
        description: "If applicable, a minimum value can be provided to signify a minimum that the value in this base has to be. This implies that if the value is less than the minimum value, the minimum value will be used instead of the value element. If the base code is PercentageMonthlySalary, the value is 3000 (which is the monthly salary) and the min value is 4000, the 4000 would be used as the value."
        type: number
      maxValue:
        description: "If applicable, a maximum value can be provided to signify a maximum that the value in this base has to be. This implies that if the value is max than the maximum value, the maximum value will be used instead of the value element. If the base code is PercentageMonthlySalary, the value is 3000 (which is the monthly salary) and the max value is 2000, the 2000 would be used as the value."
        type: number
    type: object
  ArrangementLineExtendedType:
    description: "Different rates that are applicable for this sustainable employability plan."
    required:
      - interval
    additionalProperties: false
    properties:
      lineId:
        description: "A unique identifier of this specific line element."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            $ref: "#/definitions/IdValueType"
        type: object
      amount:
        $ref: "#/definitions/AmountExtendedType"
      interval:
        $ref: "#/definitions/IntervalType"
      conditions:
        items:
          $ref: "#/definitions/ConditionType"
        type: array
      ikbReference:
        items:
          $ref: "#/definitions/IkbReferenceType"
        type: array
    type: object
  Period:
    description: "A container element that specifies the time period(s) during which the allowance is applicable. It includes a date period (which can be used for date ranges or individual dates), a time period (given the selected dates, this lets us specify the allowed times) and a weekday element that lets us specify recurring days of the week. \n\n"
    required:
      - timePeriod
    additionalProperties: false
    properties:
      datePeriod:
        items:
          $ref: "#/definitions/DatePeriod"
        type: array
      timePeriod:
        $ref: "#/definitions/TimePeriod"
      weekday:
        items:
          $ref: "#/definitions/WeekdayCodeList"
        type: array
    type: object
  ContactPersonType:
    description: "Specific people at the party who are contacts. This is the person authorized to provide the information."
    required:
      - name
    additionalProperties: false
    properties:
      name:
        $ref: "#/definitions/PersonName"
      communication:
        $ref: "#/definitions/Communication"
      roleCode:
        description: "\nThe role of the contact person. In this case the inlenersbeloning is authorized by this contact person. Therefore the role code is mainly 'Authorized by'"
        type: string
      positionTitle:
        description: "The title of the position of the contact person."
        type: string
    type: object
  EffectivePeriodType:
    description: "Indicates the validity period of this regulation. The period is from the start date up to and including the end date."
    additionalProperties: false
    properties:
      validFrom:
        description: "The start date of the validity period."
        type: string
        format: date
      validTo:
        description: "The end date of the validity period."
        type: string
        format: date
    type: object
  ArrangementType:
    description: "A container that contains the details of the leave arrangement, such as the type of leave, the amount of the leave, as well as any conditions."
    required:
      - name
      - origin
    additionalProperties: false
    properties:
      id:
        $ref: "#/definitions/Identifier"
      name:
        description: "The name or description of the leave, giving extra information that does not fit in any of the other fields."
        type: string
      origin:
        $ref: "#/definitions/OriginType"
      effectivePeriod:
        $ref: "#/definitions/EffectivePeriodType"
      paidLeave:
        items:
          $ref: "#/definitions/ArrangementLineLeaveType"
        type: array
      adv:
        items:
          $ref: "#/definitions/ArrangementLineLeaveType"
        type: array
      holidays:
        items:
          $ref: "#/definitions/ArrangementLineLeaveType"
        type: array
      specialLeave:
        items:
          $ref: "#/definitions/ArrangementLineLeaveType"
        type: array
      wazo:
        items:
          $ref: "#/definitions/ArrangementLineLeaveType"
        type: array
      mandatoryLeaveAllocation:
        $ref: "#/definitions/MandatoryLeaveType"
    type: object
  OccurrenceType:
    $ref: "./occurrence.yaml"
  TimePeriod:
    description: "The time period during which the allowance is applicable. This period spans from the start time up and including to the end time. \n\nIn the event that multiple time periods are required, multiple period elements can be provided."
    required:
      - start
      - end
    additionalProperties: false
    properties:
      start:
        description: "The start time to which the payrate is valid"
        type: string
        format: time
      end:
        description: "\nThe end time to which the payrate is valid"
        type: string
        format: time
    type: object
  IkbReferenceType:
    description: "Container element to refer how a renumeration element connects to the IKB (EN: Individual Choice Budget)"
    required:
      - relationType
      - id
    additionalProperties: false
    properties:
      relationType:
        description: "Provides the relationship between the line element and the IKB. "
        type: string
      id:
        description: "The targetId is a unique identifier that references a specific regulation or a specific rule within a regulation. It contains the id.value of the regulation."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            $ref: "#/definitions/IdValueType"
        type: object
      description:
        description: "Textbox for additional information and/ or context"
        type: string
    type: object
  AllowanceReferenceType:
    description: "This element is used indicate relations to other elements of this structure. An example of such a relation could be that an allowance is used to calculate another allowance on a compounding basis. Meaning that the the value of allowance A is calculated before and used in the calculation of the value of allowance B."
    required:
      - description
    additionalProperties: false
    properties:
      id:
        description: "The targetId is a unique identifier that references a specific regulation or a specific rule within a regulation. It contains the id.value of the regulation."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            $ref: "#/definitions/IdValueType"
        type: object
      typeCode:
        $ref: "#/definitions/AllowanceCodeType"
      description:
        description: "A description of the nature of the relation. "
        type: string
      relationType:
        description: 'The code of the type of relation that this reference is. For example "compounding". '
        enum:
          - Compounding
          - Cumulative
          - Subtractive
        type: string
    type: object
  LabourAgreements:
    description: "This container indicates in which sector the staffing customer works and which specific collective labour agreement (CLA) and/or the own employment conditions scheme is leading for the provision of remuneration information."
    additionalProperties: false
    properties:
      industryIdentifiers:
        description: "The types of industry to which the organization belongs. The industry identifiers could be referenced to using a standard taxonomy, such as the CBS - SBI 2008 (Standaard Bedrijfsindeling)."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            items:
              $ref: "#/definitions/IdValueType"
            minItems: 1
            type: array
        type: object
      collectiveLabourAgreement:
        $ref: "#/definitions/CollectiveLabourAgreement"
      customLabourAgreement:
        description: "Indicator to determine whether a custom labour agreement is active."
        type: boolean
    type: object
  DatePeriod:
    description: "Single period spanning from the start date up and including to the end date. If both start and end is provided, the date period element indicates a range of dates. If only the start date is provided, the date period element only indicates this singular date."
    required:
      - start
    additionalProperties: false
    properties:
      start:
        $ref: "#/definitions/DateDatatype"
      end:
        $ref: "#/definitions/DateDatatype"
    type: object
  MandatoryLeaveType:
    description: "A policy mandating that employees utilize a certain amount of their accrued leave within a specified period."
    additionalProperties: false
    properties:
      description:
        description: "Description of the mandatory leave allocation"
        type: string
    type: object
  ArrangementLineLimitedType:
    description: "A container that contains the details of the increase rate. It includes elements such as the amount that is provided (3% of yearly salary), the interval in which this is applied in the specified period (e.g. each year) and any conditions that might have to be met for this rate to be applicable."
    required:
      - interval
    additionalProperties: false
    properties:
      lineId:
        description: "The unique identifier of this specific remuneration line."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            $ref: "#/definitions/IdValueType"
        type: object
      amount:
        $ref: "#/definitions/AmountExtendedType"
      interval:
        $ref: "#/definitions/IntervalType"
      conditions:
        items:
          $ref: "#/definitions/ConditionType"
        type: array
    type: object
  BaseDefinitionType:
    description: "Definitions of the bases (in Dutch: 'Grondslagen') that can be used to calculate the different rates."
    required:
      - baseType
      - remunerationIndicator
      - holidayAllowanceIndicator
      - paidLeaveDayIndicator
      - allAllowancesIndicator
    additionalProperties: false
    properties:
      baseType:
        $ref: "#/definitions/BaseDefinitionCodeType"
      remunerationIndicator:
        description: "This element can be used to indicate whether remuneration(s) is/are taken into consideration for calculating this base."
        type: boolean
      holidayAllowanceIndicator:
        description: "This element can be used to indicate whether holiday allowance(s) is/are taken into consideration for calculating this base."
        type: boolean
      paidLeaveDayIndicator:
        description: "This element can be used to indicate whether paid leave days are taken into consideration for calculating this base."
        type: boolean
      allAllowancesIndicator:
        description: "This element can be used to indicate whether allowances are taken into consideration for calculating this base. If 'True', then all allowances are taken into account. If 'False', the allowances that are taken into account can be specified in the 'allowances' element."
        type: boolean
      allowances:
        items:
          description: "A list of allowances that are included in this base."
          additionalProperties: false
          properties:
            typeCode:
              $ref: "#/definitions/AllowanceCodeType"
          type: object
        type: array
      referenceDate:
        $ref: "#/definitions/OccurrenceType"
    type: object
  LegalSchemeAgencyIdType:
    description: "A government assigned identifier to distinguish the party (organization, person, or legal entity) to a transaction."
    required:
      - value
      - schemeAgencyId
    additionalProperties: false
    properties:
      value:
        $ref: "#/definitions/IdValueType"
      schemeAgencyId:
        description: "The identification of the agency that manages the identifier scheme."
        enum:
          - KvK
          - OIN
          - RSIN
        type: string
    type: object
  PartyType:
    description: "The customer/hirer of the worker."
    required:
      - legalId
      - personContacts
    additionalProperties: false
    properties:
      id:
        items:
          $ref: "#/definitions/Identifier"
        maxItems: 2
        type: array
      name:
        description: "The name of the customer."
        type: string
      legalId:
        items:
          $ref: "#/definitions/LegalSchemeAgencyIdType"
        minItems: 1
        type: array
      personContacts:
        items:
          $ref: "#/definitions/ContactPersonType"
        minItems: 1
        type: array
    type: object
  SalaryScaleType:
    description: "Base salary of a job or of an employee including minimum and maximum salary and certain salary steps. "
    required:
      - name
      - currency
    additionalProperties: false
    properties:
      name:
        description: "The name or label of the salary scale. E.g., 1,2,3."
        type: string
      maxValue:
        description: "The maximum value of the salary scale."
        type: number
      minValue:
        description: "The minimum value of the salary scale."
        type: number
      currency:
        description: "The three‑letter currency code defined by the ISO 4217 standard (e.g., “EUR”, “USD”)."
        type: string
      salaryStep:
        items:
          $ref: "#/definitions/SalaryScaleStepType"
        type: array
      careerLevel:
        description: "Indicates whether work experience is considered in the salary scale, with a true or false value and a possible description."
        additionalProperties: false
        properties:
          indicator:
            description: "A boolean indicating the presence of taking into account classification of career levels."
            type: boolean
          description:
            description: "Description of the career levels."
            type: string
        type: object
      positionProfileReference:
        items:
          description: "Specifies the position profile that this salary scale is applied to. This element refers to the 'positionId/value' element in the corresponding position profile. "
          required:
            - positionId
          additionalProperties: false
          properties:
            positionId:
              description: "A unique identifier referencing the specific role or job profile as defined in the organization’s salary structure (NL: Salarishuis). This ID enables consistent linkage between the employee’s position and the corresponding salary scale and classification rules."
              required:
                - value
              additionalProperties: false
              properties:
                value:
                  $ref: "#/definitions/IdValueType"
              type: object
            startSalaryStep:
              description: "A code or numeric value indicating the initial salary step assigned to the role or employee upon entry into the salary scale. It defines the starting point within the organization’s structured salary progression."
              type: string
              format: uri-reference
            description:
              description: " A free‑text field for capturing additional details related to the remuneration starting position (NL: Inschaling), such as exceptions, special conditions, or justifications influencing the initial salary step or profile assignment."
              type: string
          type: object
        type: array
    type: object
  PersonName:
    description: "A component for capturing summary or fine-grain data comprising a person's name."
    required:
      - formattedName
    additionalProperties: false
    properties:
      formattedName:
        description: "The formatted name of a person, as it would be written out together (for example, on a mailing label)."
        type: string
    type: object
  PositionProfile:
    description: "These are the position profiles for which the inquiry pay equity applies. From the salary table and other arrangements, you can also use these positions for the conditions of those arrangements."
    required:
      - positionId
      - positionTitle
      - origin
    additionalProperties: false
    properties:
      positionId:
        description: "The primary identifier of the position profile, typically from the system of record."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            $ref: "#/definitions/IdValueType"
        type: object
      positionTitle:
        description: "The title of the position, which is a short phrase describing the position."
        type: string
      origin:
        $ref: "#/definitions/OriginType"
      referenceTitle:
        $ref: "#/definitions/IdValueType"
      workDescription:
        description: "The free text description of the work that this position normally requires."
        type: string
    type: object
  Communication:
    description: "The communication channels (phone, email) to contact and reach the contact person."
    additionalProperties: false
    properties:
      phone:
        description: "Array of phone information details."
        required:
          - formattedNumber
        additionalProperties: false
        properties:
          formattedNumber:
            description: "The formatted formatted phone number according to the E.164 standard. For example +31 612345678, where +31 is the country code (1 to 3 digits) and 612345678 is the subscriber telephone number (maximum 12 digits)."
            type: string
        type: object
      email:
        description: "Array of email information details."
        required:
          - address
        additionalProperties: false
        properties:
          address:
            description: "The actual email address."
            type: string
        type: object
    type: object
  SalaryScaleStepType:
    description: " Specifies the steps within each salary scale, where each step has a name/label, a salary value, and possible conditions."
    required:
      - name
      - value
    additionalProperties: false
    properties:
      name:
        description: "The name or label of the salary step. E.g., 1,2,3."
        type: string
      value:
        description: "The value of the step."
        type: number
      minimumWage:
        description: "A boolean that indicates whether this salary step is equal to the minimum wage. "
        type: boolean
      conditions:
        items:
          $ref: "#/definitions/ConditionType"
        type: array
    type: object
  CollectiveLabourAgreement:
    description: "Information about the collective labour agreement (CLA; CAO in Dutch) that determines the renumeration. In the Netherlands this can be the CLA of the ABU, NBBU or a specific CLA of the staffing customer."
    required:
      - name
      - id
    additionalProperties: false
    properties:
      name:
        description: "The name of the collective labour agreement (CLA)."
        type: string
      id:
        description: "The identifier of the collective labour agreement (CLA)."
        required:
          - value
        additionalProperties: false
        properties:
          value:
            $ref: "#/definitions/IdValueType"
        type: object
      effectivePeriod:
        $ref: "#/definitions/EffectivePeriodType"
      basedOn:
        description: "A collective labour agreement (CLA) applies if the employer is a member of the industry association, if the CLA has been declared generally binding for the sector by the Minister of Social Affairs and Employment, or if it is declared applicable in the employee's employment contract."
        type: string
    type: object
  WaitingDaysType:
    description: "The amount of waiting days (NL: wachtdagen) that the worker is subjected to before being eligible to receive sick pay."
    required:
      - value
      - unitCode
    additionalProperties: false
    properties:
      value:
        description: "The value of the amount expressed in number of days."
        type: number
      conditions:
        items:
          $ref: "#/definitions/ConditionType"
        type: array
      unitCode:
        description: "This element provides a way to specify the type of unit of the value. This can be for example 'percentage' if the unit type of the value is to be interpreted as a percentage or euro if the unity type of the value is to be interpreted as a euro monetary amount."
        enum:
          - Day
          - Hour
        type: string
        $ref: "#/definitions/IntervalCodeType"
    type: object
  BaseDefinitionCodeType:
    type: string
  IdValueType:
    type: string
  BaseUnitCodeType:
    type: string
  ConditionType:
    $ref: "./condition.yaml"
  DateDatatype:
    type: string
    format: date
  IntervalCodeType:
    type: string
  AmountUnitCodeType:
    enum:
      - Hour
      - Percentage
      - Euro
    type: string
  SchemeAgencyIdType:
    enum:
      - Customer
      - Supplier
    type: string
  AmountUnitCodeExtendedType:
    enum:
      - Hour
      - Percentage
      - SalaryStep
      - Euro
      - Day
    type: string
  IntervalUnitCodeType:
    enum:
      - Hour
    type: string
  AllowanceCodeType:
    type: string
  WeekdayCodeList:
    enum:
      - Holiday
      - Friday
      - Monday
      - Saturday
      - Sunday
      - Thursday
      - Tuesday
      - Wednesday
    type: string
